// +build mock
// Code generated by server_generator. DO NOT EDIT.
// generated version: {{ .AppVersion }}

package {{ .Package }}

import (
	"net/http"

	"github.com/labstack/echo/v4"

	props "{{ .ControllerPropsPackage }}"
)

// MockRoutes ...
type MockRoutes struct {
	router *echo.Group
}

// apiGenMockOption api-gen mock options
type apiGenMockOption struct {
	FilePrefix string `json:"file_prefix"`
}

// NewRoutes ...
func NewMockRoutes(p *props.ControllerProps, router *echo.Group, jsonDir string) *MockRoutes {
	r := &MockRoutes{
		router: router,
	}
{{- range .Controllers }}
	router.{{ .HTTPMethod }}("{{ .Endpoint }}", r.{{ .HandlerName }}(p, jsonDir))
{{- end }}
	return r
}
{{ range .Controllers }}
// {{ .HandlerName }} ...
func (r *MockRoutes) {{ .HandlerName }}(p *props.ControllerProps, jsonDir string) echo.HandlerFunc {
	type Mock struct {
		Payload {{ .ResponseStructName }} `json:"payload"`
	}
	return func(c echo.Context) error {
		req := new({{ .RequestStructName }})
		if err := c.Bind(req); err != nil {
			return c.JSON(http.StatusBadRequest, map[string]interface{}{
				"code":    http.StatusBadRequest,
				"message": "invalid request.",
			})
		}

		option := &apiGenMockOption{}
		xago := c.Request().Header.Get("X-Api-Gen-Option")
		if xago != "" {
			if err := json.Unmarshal([]byte(xago), option); err != nil {
				return c.JSON(http.StatusBadRequest, map[string]interface{}{
					"code":    http.StatusBadRequest,
					"message": "invalid X-Api-Gen-Option.",
				})
			}
		}

		loadJsonFile := "{{ GetDefaultJsonFile . }}.json"
		if option.FilePrefix != "" {
			loadJsonFile = option.FilePrefix
		}
		if !strings.HasSuffix(loadJsonFile, ".json") {
			loadJsonFile = fmt.Sprintf("{{ GetDefaultJsonFile . }}_%s.json", option.FilePrefix)
		}
		loadJsonPath := filepath.Join(jsonDir, loadJsonFile)
		log.Printf("{{ .HTTPMethod }}: load %s", loadJsonPath)
		j, err := ioutil.ReadFile(loadJsonPath)
		if err != nil {
			m := fmt.Sprintf("load mock json error in %s: %+v", loadJsonPath, err)
			log.Printf(m)
			return c.JSON(http.StatusInternalServerError, map[string]interface{}{
				"code":    http.StatusInternalServerError,
				"message": m,
			})
		}

        mock := new(Mock)
		err = json.Unmarshal(j, mock)
		if err != nil {
			m := fmt.Sprintf("unmarshal mock json error in %s: %+v", loadJsonPath, err)
			log.Printf(m)
			return c.JSON(http.StatusInternalServerError, map[string]interface{}{
				"code":    http.StatusInternalServerError,
				"message": m,
			})
		}

		return c.JSON(http.StatusOK, mock.Payload)
	}
}
{{- end }}
